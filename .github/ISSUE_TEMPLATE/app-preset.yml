---
name: App launch preset
description: Request one Sunshine launch option for a non-game app.
title: '[APP PRESET]: '
labels:
  - request-app-preset
body:
  - type: markdown
    attributes:
      value: |
        Apps have no GameDB record. A maintainer reviews the official URL and command before approval.
        Submit one host OS and launch option per issue.
  - type: input
    id: app_name
    attributes:
      label: App name
      description: Official app name.
    validations:
      required: true
  - type: input
    id: app_url
    attributes:
      label: Official app URL
      description: HTTPS homepage or official source repository.
    validations:
      required: true
  - type: input
    id: app_image_url
    attributes:
      label: App image URL
      description: Optional HTTPS image URL for the catalog. Do not enter a local file path.
  - type: dropdown
    id: os
    attributes:
      label: Host operating system
      options:
        - Windows
        - Linux
        - macOS
    validations:
      required: true
  - type: dropdown
    id: method
    attributes:
      label: Launch method
      options:
        - Native
        - Steam
        - Epic Games
        - GOG
        - Emulator
        - Other
    validations:
      required: true
  - type: input
    id: preset_name
    attributes:
      label: Preset name
      description: Short display name for this launch option.
    validations:
      required: true
  - type: input
    id: command
    attributes:
      label: Command
      description: |
        Use {{HOME}} for a variable path. Windows also allows {{SYSTEM_DRIVE}}, {{PROGRAM_FILES}}, and
        {{PROGRAM_FILES_X86}}. Steam launcher URIs are stored as Sunshine detached commands automatically.
    validations:
      required: true
  - type: input
    id: working_dir
    attributes:
      label: Working directory
      description: Optional Sunshine working-dir value.
  - type: textarea
    id: notes
    attributes:
      label: Notes
      description: Setup instructions or prerequisites.
  - type: textarea
    id: replacement_reason
    attributes:
      label: Replacement reason
      description: Explain why the existing preset should change. Required with the issue number below.
  - type: input
    id: replacement_issue
    attributes:
      label: Preset to replace (issue number)
      description: Optional issue number shown with the published preset. Leave blank to add a new preset.
